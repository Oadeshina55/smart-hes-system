# DLMS Gateway Configuration

# TCP Port for DLMS meter connections
DLMS_PORT=4000

# Gateway host (0.0.0.0 for all interfaces)
GATEWAY_HOST=0.0.0.0

# Backend Socket.IO URL
BACKEND_URL=http://localhost:5000

# Backend API URL (for REST calls if needed)
BACKEND_API_URL=http://localhost:5000/api
